export interface ISumSubCallbackBody {
  applicantId: string;
  // inspectionId: string;
  // correlationId: string;
  // jobId: string;
  type: string;
  // review: {
  //   reviewAnswer: SumSubVerificationLabels,
  //   label: string; // 'OTHER',
  //   rejectLabels: string[]; // ['DUPLICATE'],
  //   reviewRejectType: SumsubReviewRejectType;
  // };
  // success: boolean;
}